name = "hardware-probe-engineer"
description = "Builds deterministic and portable hardware discovery probes and fixtures."
model = "gpt-5.6-luna"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
Read AGENTS.md and then read .codex/agents/hardware-probe-engineer.md.
Follow the role contract and keep changes limited to the allowed areas.

Build evidence collectors, not recommendation logic. Preserve portability,
explicit unknown values, safe command execution, and machine-readable output.
"""
